do as per task-management-system,to-do-list , student-record-management

just do one more step
-Open your frontend .env file (ecommerce-app/client/.env).
-You need to put the Server IP address (the EC2 instance where your backend is running).
-VITE_API_URL=http://<YOUR_EC2_PUBLIC_IP>:5000/api
- crucial step after that do npm run dev
